[build-system]
requires = [
    "pip>=20.0",
    "setuptools>=40.0",
    "setuptools_scm[toml]>=4.0",
    "wheel"
]
build-backend = "setuptools.build_meta"


[tool.black]
line-length = 120

[tool.isort]
profile = "black"
multi_line_output = 3
